services:
  - type: web
    name: Luna
    env: docker
    region: Singapore
    plan: free
    envVars:
      - key: BOT_TOKEN
        # value: BOT_TOKEN
        sync: false
      - key: CONFIG_FILE_URL
        # value: DATABASE_URL
        sync: false
      - key: GDRIVE_ID
        # value: OWNER_ID
        sync: false
      - key: UPSTREAM_REPO
        sync: false
      - key: UPSTREAM_BRANCH
        sync: false
